#include "shell_libs.h"

int run_command(char **list)
{
	if (execve(list[0], list, NULL) == -1)
		printf("%s: 1: %s: not found\n", argv[0], list[0]);
	return (0);
}

int run_multiprocess(char **list)
{
	for (int i = 0; i < 5; i++)
	{
		runchildproc(list, 3);
	}
	return (0);
}
